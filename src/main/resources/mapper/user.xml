<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.userDao">
    <insert id="Regist" parameterType="String" >

    </insert>
    <select id="Login" parameterType="String" resultType="com.pojo.user">
        SELECT username and password FROM ESP_USER where username=#{username}
    </select>
    <update id="changePWD">

    </update>
    <select id="getBalance">

    </select>
    <select id="getPoint">

    </select>

</mapper>
