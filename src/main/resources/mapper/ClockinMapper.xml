<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.ClockinDao">

    <select id="getTotalById" parameterType="int" resultType="int">Q
        SELECT total_number
        FROM esp_clockin
        WHERE user_id=#{userId} and courses_id=#{coursesNumber}
    </select>
    <select id="getCurrentById" parameterType="int" resultType="int">
        SELECT corrent_clockin_number
        FROM esp_clockin
        WHERE user_id=#{userId} and courses_id=#{coursesNumber}
    </select>

    <update id="clockin" parameterType="int"  >
        UPDATE esp_clockin
        SET corrent_clockin_number = 1+corrent_clockin_number
        WHERE user_id=#{userId} and courses_id=#{coursesNumber}
    </update>


</mapper>
