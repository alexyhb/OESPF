<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.CoursesDao">

    <insert id="addCourses">
      INSERT INTO esp_courses
      (courses_name,img, text,exam_type, courses_length, courses_number, money ,teach_by)
       VALUES
      (#{coursesName},#{img},#{text},#{examType},#{coursesLength},#{coursesNumber},#{money},#{teachBy})
    </insert>
    <delete id="deleteCourses" parameterType="Courses">
        DELETE FROM esp_courses WHERE id=#{id}
    </delete>
    <select id="getCousesListByType" resultType="com.pojo.Courses">
        SELECT *
        FROM esp_courses
        WHERE exam_type=#{examType}
        ORDER BY id ASC
    </select>
    <select id="getSpInfo" resultType="com.pojo.Courses">
        SELECT *
        FROM esp_courses
        WHERE id=#{id}
    </select>
    <select id="getCoursesList" resultType="com.pojo.Courses">
        SELECT  *
        FROM esp_courses
        ORDER BY id ASC
    </select>
    <update id="setCourses">
        UPDATE esp_courses
        SET courses_name=#{coursesName},img=#{img}, text=#{text},exam_type=#{examType}, courses_length=#{coursesLength}, courses_number=#{coursesNumber}, money=#{money} ,teach_by=#{teachBy}
        where id=#{id}
    </update>

</mapper>
